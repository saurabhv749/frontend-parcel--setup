{
  "name": "frontend-dev-setup",
  "version": "1.0.0",
  "description": "contains the basic frontend development setup using parcel for bundling and serve to test out the production build",
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "parcel build --no-source-maps --dist-dir public ./src/index.html",
    "prod": "serve -C -c serve.json",
    "test": "npm run build && npm run prod"
  },
  "keywords": [
    "portfolio",
    "react",
    "nextUI"
  ],
  "author": "saurabh verma",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.10.4",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-router-dom": "^6.3.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.5.0"
  }
}
